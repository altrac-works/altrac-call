CREATE TABLE visits (
    timestamp text,
    ip text,
    source text,
    campaign text,
    ocdids text
);

CREATE TABLE clicks (
    timestamp text,
    ip text,
    source text,
    campaign text,
    tag text,
    ocdid text
);
